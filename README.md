# sandbox

Throwaway target for harness-change dry runs. Improvement proposals (per
the autonomous-loop self-improvement loop) must pass a sandbox mission
here before promotion to ADR.

Run tests: `python -m pytest test_hello.py -q`
